---
name: ruff-setup
description: PythonプロジェクトでRuffを導入・設定する。uvでのインストール、ruff.toml配置、.gitignore更新のときに使う。
---

# Ruff セットアップ

## 導入と設定
- `references/ruff-setup.md` を参照してインストール、設定、`.gitignore` 更新を行う。
- `assets/ruff.toml` をプロジェクトルートへコピーし、必要に応じて調整する。
