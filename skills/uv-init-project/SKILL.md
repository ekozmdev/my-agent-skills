---
name: uv-init-project
description: uvでPythonプロジェクトを初期化する。uv initの実行、生成物の確認、pyproject.tomlのadd-bounds設定時に使う。
---

# uvによるPythonプロジェクト初期化

## 新規プロジェクトの初期化
- `references/uv-init.md` を参照して `uv init` のオプション、生成物の確認、`pyproject.toml` 初期設定を行う。

## 関連スキル
- 依存管理は `uv-manage-dependencies` を使う。

## 注意
- uv コマンドが見つからない場合はユーザーに確認する。
